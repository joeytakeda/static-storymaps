{
  "name": "StaticStoryMaps",
  "version": "0.1.0",
  "description": "Experimental library for self-hosting KnightLab StoryMaps",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "image-size": "^0.9.7",
    "yaml": "^1.10.2"
  },
  "scripts": {
    "cleanup": "rm -rf public && mkdir public",
    "move": "mv dist public && cp -r src public/src && cp -r index.html public/index.html",
    "localize": "node localize.js",
    "build": "npm run cleanup && npm run localize && npm run move",
    "dev": "npm run localize && npm run move"
  }
}
